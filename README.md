# To-Do List Application (Technical Test)

This is a full-stack web application for task management, built with Django REST Framework (Backend) and React (Frontend).

## Architecture & Tech Stack

- **Frontend**: React, Vite, Node.js
- **Backend**: Python, Django, Django REST Framework
- **Database**: PostgreSQL
- **Containerization**: Docker, Docker Compose
- **Testing**: Pytest (Backend), Selenium (Frontend)
- **CI/CD**: GitHub Actions (planned)

## Project Initialization

### Prerequisites
- Docker and Docker Compose installed.

### Setup Instructions
1. Clone the repository.
2. In the root directory, run:
   ```bash
   docker-compose up --build
   ```
3. The frontend will be available at `http://localhost:5173`.
4. The backend will be available at `http://localhost:8000`.

*(More details, such as migrations and testing instructions, will be added as features are developed.)*
